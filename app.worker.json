{
  "healthchecks": {
    "worker": [
      {
        "type": "liveness",
        "name": "worker liveness check",
        "description": "Ongoing liveness check for worker process",
        "path": "/livez",
        "attempts": 3,
        "timeout": 5,
        "wait": 10
      },
      {
        "type": "readiness",
        "name": "worker readiness check",
        "description": "Check if the worker process is ready (database connected)",
        "path": "/health",
        "attempts": 3,
        "timeout": 5,
        "wait": 10
      },
      {
        "type": "startup",
        "name": "worker startup check",
        "description": "Check if the worker process is responding during startup",
        "path": "/health",
        "attempts": 5,
        "timeout": 10,
        "wait": 5
      }
    ]
  }
}
