name: Deploy Worker to Staging

env:
  APP_NAME: rabbithq-worker-staging
  BACKEND_APP_NAME: rabbit-hq-staging

on:
  push:
    branches: [main]
    paths:
      - "apps/back-end/**"
      - "apps/back-end/app.worker.json"
      - ".github/workflows/deploy-worker-staging.yml"
  workflow_dispatch:

jobs:
  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔍 Type Check
        working-directory: apps/back-end
        run: npm run type-check

      - name: 🧹 Lint
        working-directory: apps/back-end
        run: npm run lint

      - name: 💅 Format Check
        working-directory: apps/back-end
        run: npm run format

      - name: 🧪 Run Tests
        working-directory: apps/back-end
        run: npm run test:run

  configure-worker-app:
    name: Configure Worker App
    runs-on: ubuntu-latest
    environment: staging
    needs: quality-checks

    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DOKKU_SSH_PRIVATE_KEY }}

      - name: 🏗️ Add SSH known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ vars.DOKKU_HOST }} >> ~/.ssh/known_hosts

      - name: 📋 Copy config from backend app
        run: |
          echo "Exporting config from backend app..."
          CONFIG=$(ssh dokku@${{ vars.DOKKU_HOST }} config:export ${{ env.BACKEND_APP_NAME }})
          echo "Importing config to worker app..."
          # Remove "export " prefix from each line and set config variables
          # Exclude DOKKU_APP_RESTORE and database URLs as we'll set them via postgres:link
          echo "$CONFIG" | sed 's/^export //' | grep -v "^DOKKU_APP_RESTORE=" | grep -v "^DATABASE_URL=" | grep -v "^DOKKU_POSTGRES" | while IFS= read -r line; do
            if [ -n "$line" ]; then
              ssh dokku@${{ vars.DOKKU_HOST }} config:set ${{ env.APP_NAME }} "$line" || true
            fi
          done

      - name: 🚫 Configure worker app to use app.worker.json
        run: |
          echo "Configuring worker app to use app.worker.json (without predeploy hook)..."
          ssh dokku@${{ vars.DOKKU_HOST }} app-json:set ${{ env.APP_NAME }} appjson-path app.worker.json

  deploy-worker:
    name: Deploy Worker to Dokku - Staging
    runs-on: ubuntu-latest
    environment: staging
    needs: [quality-checks, configure-worker-app]

    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full history for proper git operations

      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DOKKU_SSH_PRIVATE_KEY }}

      - name: 🏗️ Add SSH known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ vars.DOKKU_HOST }} >> ~/.ssh/known_hosts

      # Deploy the application
      - name: 🐳 Deploy to Dokku
        uses: dokku/github-action@master
        with:
          git_remote_url: "ssh://dokku@${{ vars.DOKKU_HOST }}:22/${{ env.APP_NAME }}"
          ssh_private_key: ${{ secrets.DOKKU_SSH_PRIVATE_KEY }}
          git_push_flags: "--force"

      - name: 🔍 Verify worker app status
        run: |
          echo "Checking worker app status..."
          ssh dokku@${{ vars.DOKKU_HOST }} ps:report ${{ env.APP_NAME }}

      - name: 📈 Scale processes (disable web, enable worker)
        run: |
          echo "Scaling processes: web=0, worker=1..."
          ssh dokku@${{ vars.DOKKU_HOST }} ps:scale ${{ env.APP_NAME }} web=0 worker=1 || \
            echo "Note: Process scaling may need to be done manually"

      - name: 📋 Show worker logs
        run: |
          echo "Recent worker logs:"
          ssh dokku@${{ vars.DOKKU_HOST }} logs ${{ env.APP_NAME }} --tail 20 || true

      - name: ✅ Deployment complete
        run: |
          echo "🎉 Worker deployment to staging completed successfully!"
          echo "📝 Commit: ${{ github.sha }}"
          echo "🔍 Worker app: ${{ env.APP_NAME }}"
          echo "💡 View logs: ssh dokku@${{ vars.DOKKU_HOST }} logs ${{ env.APP_NAME }}"
