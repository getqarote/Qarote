name: Deploy Landing to Production

on:
  workflow_dispatch:
    inputs:
      confirm_production:
        description: 'Type "DEPLOY" to confirm production deployment'
        required: true
        default: ""

jobs:
  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest
    env:
      # Turbo remote cache (Vercel free tier). Scoped to this job so the token
      # is not exposed to the deploy job, which runs third-party SSH actions.
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: "9.0.0"

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔍 Check package version consistency
        run: pnpm run syncpack:check

      - name: 🔍 Type Check
        run: pnpm turbo run type-check --filter=qarote-web

      - name: 🔍 Knip (unused code check)
        run: pnpm run knip

      - name: 🧹 Lint
        run: pnpm turbo run lint --filter=qarote-web

      - name: 💅 Format Check
        run: pnpm turbo run format --filter=qarote-web

  deploy-landing:
    name: Deploy Landing to Cloudflare Pages - Production
    environment: production
    runs-on: ubuntu-latest
    needs: quality-checks
    if: github.event.inputs.confirm_production == 'DEPLOY'

    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: "9.0.0"

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts # Ignore scripts to avoid husky errors

      - name: 📦 Build workspace packages
        run: pnpm turbo run build --filter=@qarote/i18n --filter=@qarote/analytics --filter=@qarote/og-cards

      - name: Install Wrangler CLI globally
        run: npm install -g wrangler@3.90.0

      - name: 🏗️ Build landing page for production
        working-directory: apps/web
        env:
          VITE_APP_BASE_URL: ${{ vars.VITE_APP_BASE_URL }}
          VITE_PORTAL_URL: ${{ vars.VITE_PORTAL_URL }}
          PUBLIC_POSTHOG_PROJECT_TOKEN: ${{ secrets.POSTHOG_API_KEY }}
          PUBLIC_POSTHOG_HOST: ${{ vars.POSTHOG_HOST }}
          VITE_ENVIRONMENT: production
          NODE_ENV: production
        run: pnpm run build

      - name: 🚀 Deploy to Cloudflare Pages - Production
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy apps/web/dist --project-name=qarote-landing-production --branch=main --commit-hash=${{ github.sha }}

      - name: ✅ Deployment Success
        run: |
          echo "🎉 Landing page deployed successfully to production!"
          echo "🌐 Production URL: https://qarote-landing-production.pages.dev"
          echo "🔗 Custom Domain: https://qarote.io (if configured)"
          echo "📝 Commit: ${{ github.sha }}"
