name: Deploy Portal to Staging

on:
  push:
    branches: [main]
    paths:
      - "apps/portal/**"
      - ".github/workflows/deploy-portal-staging.yml"
  workflow_dispatch:

jobs:
  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔍 Check package version consistency
        run: npm run syncpack:check

      - name: 🔍 Type Check
        working-directory: apps/portal
        run: npm run type-check

      - name: 🧹 Lint
        working-directory: apps/portal
        run: npm run lint
        continue-on-error: false

      - name: 💅 Format Check
        working-directory: apps/portal
        run: npm run format

  deploy-portal:
    name: Deploy Portal to Cloudflare Pages - Staging
    environment: staging
    runs-on: ubuntu-latest
    needs: quality-checks

    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: 📦 Install dependencies
        run: npm ci --ignore-scripts # Ignore scripts to avoid husky errors

      - name: 🏗️ Build portal
        working-directory: apps/portal
        env:
          VITE_API_URL: ${{ vars.VITE_API_URL }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          VITE_ENABLE_OAUTH: ${{ vars.VITE_ENABLE_OAUTH }}
          VITE_DEPLOYMENT_MODE: ${{ vars.VITE_DEPLOYMENT_MODE }}
          NODE_ENV: production
        run: npm run build

      - name: 🚀 Deploy to Cloudflare Pages - Staging
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy apps/portal/dist --project-name=qarote-portal-staging --branch=main --commit-hash=${{ github.sha }}

      - name: ✅ Deployment Success
        run: |
          echo "🎉 Portal deployed successfully to staging!"
          echo "🌐 Staging URL: https://qarote-portal-staging.pages.dev"
          echo "🔗 Custom Domain: https://portal.qarote.io (if configured)"
          echo "📝 Commit: ${{ github.sha }}"
